# Next.js frontend

See the [root development guide](../../README.md) for setup, environment variables, commands, and verified limitations.

Frontend only; use the NestJS REST API through the centralized API configuration. Local URL: http://localhost:3000. Never expose backend secrets here.
