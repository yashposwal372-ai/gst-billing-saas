NODE_ENV=development
PORT=4000
FRONTEND_URL=http://localhost:3000
# Development-only credentials matching compose.yaml.
DATABASE_URL=postgresql://gst_dev:gst_dev_local@localhost:5432/gst_billing
REDIS_URL=redis://localhost:6379
# Unused until Phase 2. Required to be distinct, non-placeholder values
# of at least 32 characters in production. Never commit real secrets.
JWT_SECRET=
JWT_REFRESH_SECRET=
